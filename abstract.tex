(Esbozo) In this project, we are developing a program that generates C++ code based on received variables. The resulting executables, given the previously selected restrictions and options, will output a Legendre pair of the desired length.